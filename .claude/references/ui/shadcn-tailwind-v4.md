# shadcn (new-york) + Tailwind v4 참조 (스텁)

> **상태**: TBD — 추후 작성 예정.
> 현재는 `ui-design-reviewer` 에이전트가 `docs/guides/styling-guide.md` 와 `docs/guides/component-patterns.md` 를 직접 인용합니다.

## 채워질 내용 (예정)

- Tailwind v4 `@theme` 토큰 시스템 (color/spacing/radius) — baseline 정의값
- 다크모드 토큰 매핑 (`bg-background` / `text-foreground` / `border-border`)
- shadcn new-york 스타일 컴포넌트 사용 컨벤션 (variant prop / asChild / forwardRef)
- 접근성 체크리스트 (Dialog/Sheet/Combobox 등 흔한 누락 패턴)
- `cn()` 유틸 사용 규칙 (조건부 className 결합)
- 반응형 브레이크포인트 baseline 합의 (`sm`/`md`/`lg`/`xl` 의미)

> 채워지기 전까지는 일반론으로 채우지 _말 것_ — `ui-design-reviewer`가 "주참조 없음"을 보고하도록 비워둔다.
